# Assets

This folder stores shared images, diagrams, screenshots, exported charts, and supporting media used across internship tasks and documentation.

Keep task-specific generated outputs inside the corresponding task folder unless the asset is reused across the repository.
